# Backend / local-only (not shipped to client via this repo)
honeybee_admin/
honeybee-admin/
*.zip

# Dependencies & build
node_modules/
frontend/node_modules/
frontend/dist/

# Env secrets
.env
.env.local
frontend/.env
frontend/.env.local

# PHP / OS
honeybee_admin/storage/sessions/
.DS_Store
*.log
